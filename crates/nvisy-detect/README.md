# nvisy-detect

Detection and redaction plugin for the Nvisy runtime. Provides regex-based entity detection, checksum validation, policy evaluation, classification, audit emission, and file loaders for plaintext, CSV, and JSON formats.
