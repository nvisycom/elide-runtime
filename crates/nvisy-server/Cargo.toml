# https://doc.rust-lang.org/cargo/reference/manifest.html

[package]
name = "nvisy-server"
description = "HTTP server exposing the nvisy Engine pipeline via REST endpoints"
keywords = ["nvisy", "server", "http", "api"]
categories = ["web-programming::http-server"]
readme = "README.md"

version = { workspace = true }
rust-version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
publish = { workspace = true }

authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
documentation = { workspace = true }

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[[bin]]
name = "nvisy-server"
path = "src/main.rs"

[dependencies]
# Internal crates
nvisy-core = { workspace = true, features = [] }
nvisy-engine = { workspace = true, features = [] }
nvisy-identify = { workspace = true, features = [] }
nvisy-ontology = { workspace = true, features = [] }

# HTTP framework
axum = { workspace = true, features = ["json", "multipart", "tokio"] }
aide = { workspace = true, features = ["axum", "axum-json", "scalar", "macros"] }
tower = { workspace = true, features = ["util"] }
tower-http = { workspace = true, features = ["trace", "cors", "timeout", "request-id", "util"] }

# (De)serialization
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true, features = [] }
schemars = { workspace = true, features = [] }

# Async runtime
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "signal"] }

# Primitive datatypes
uuid = { workspace = true, features = [] }

# Observability
tracing = { workspace = true, features = [] }
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }

# Encoding
base64 = { version = "0.22", features = [] }

# Error handling
thiserror = { workspace = true, features = [] }
