# https://doc.rust-lang.org/cargo/reference/manifest.html

[package]
name = "nvisy-server"
description = "Axum HTTP server for the Nvisy data protection platform"
keywords = ["nvisy", "server", "http", "axum"]
categories = ["web-programming::http-server"]

version = { workspace = true }
rust-version = { workspace = true }
edition = { workspace = true }
license = { workspace = true }
publish = { workspace = true }

authors = { workspace = true }
repository = { workspace = true }
homepage = { workspace = true }
documentation = { workspace = true }

[[bin]]
name = "nvisy-server"
path = "src/main.rs"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
# Internal crates
nvisy-core = { workspace = true, features = ["schema"] }
nvisy-engine = { workspace = true, features = ["schema"] }
nvisy-detect = { workspace = true, features = [] }
nvisy-object = { workspace = true, features = [] }
nvisy-python = { workspace = true, features = [] }

# JSON Schema generation
schemars = { workspace = true }

# (De)serialization
serde = { workspace = true, features = ["derive"] }
serde_json = { workspace = true, features = [] }

# Async runtime
tokio = { workspace = true, features = ["rt-multi-thread", "macros", "signal"] }

# HTTP server
axum = { workspace = true, features = ["http2", "macros"] }
tower = { workspace = true, features = ["full"] }
tower-http = { workspace = true, features = ["cors", "trace", "request-id", "limit"] }

# OpenAPI / Documentation
utoipa = { workspace = true, features = ["axum_extras"] }
utoipa-swagger-ui = { workspace = true, features = ["axum"] }

# Primitive datatypes
uuid = { workspace = true, features = ["v4"] }
chrono = { workspace = true, features = [] }

# Observability
tracing = { workspace = true, features = [] }
tracing-subscriber = { workspace = true, features = ["fmt", "ansi", "json", "env-filter"] }

# Error handling
thiserror = { workspace = true, features = [] }
anyhow = { workspace = true, features = ["backtrace"] }
