# nvisy-server

Axum-based HTTP server for the Nvisy runtime. Exposes REST endpoints for graph execution, data redaction, policy management, and audit log queries, with dependency-injected service layer.
