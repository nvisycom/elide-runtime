[project]
name = "nvisy-hf"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "transformers>=4.0",
    "torch>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.backends"

[tool.hatch.build.targets.wheel]
packages = ["src/nvisy_hf"]
